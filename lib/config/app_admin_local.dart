/// Email admin tunggal — ganti dengan akun Firebase Anda (Authentication).
///
/// Deploy Firestore: sesuaikan juga `adminEmail()` di `firestore.rules`.
const String kAppAdminEmailLocal = 'admin@preloved.id';
